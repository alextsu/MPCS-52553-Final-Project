<h2>Add a University</h2>
<h3>Create a New University Profile:</h3>
<% if @university.errors.any? %>
	<ul class="errors">
		<% @university.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
		<% end %>
 	</ul>
<% end %>

<%= form_tag "/universities", :method => 'post' do %>

	<p>
		<%= label_tag :name %>
		<%= text_field_tag :name %>
	</p>

	<p>
		<%= label_tag :color %>
		<%= text_field_tag :color %>
	</p>

	<p>
		<label>Location </label>
		<select name="location">
			<% @locations.each do |location| %> 
			<option><%= location.city%></option>
			<% end %>
		</select>
	</p>
	
	<%= submit_tag "Create New University" %>

<% end %>
<p><%= link_to "Back to University page", "/universities" %> </p>




